import React from 'react';
import { ListGroupItem, ListGroupItemProps } from "@morenobr/guideline-react-unstyled/List";

export default (props:ListGroupItemProps) => {
  return (
    <ListGroupItem {...props} />
  );
}
